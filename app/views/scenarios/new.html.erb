<% provide(:title, 'Appliances') %>
<% if @records.any? %>
    <div class="panel-group" id="accordion" role="tablist" aria-multiselectable="true">
      <%= render partial: "entries" %>
    </div>
<% else %>
    <p class="no-entries">Nemáš uložené žiadne elektrické spotrebiče...</p>
<% end %>